#include <stdio.h>
#include <stdlib.h>

int main()
{   int num;
int x;
    printf("enter a number");
    scanf("%d",&num);
    do{printf("%d\n",num);
        num++;
    }while(x=num;x<num+6;x++);

    return 0;
}
